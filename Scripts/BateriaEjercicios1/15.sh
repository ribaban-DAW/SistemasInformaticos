#!/bin/bash

# Realiza un shell-script que limpie la pantalla, muestre todos los nombres
# de ficheros que hay en el directorio actual (mostrando sus atributos),
# la fecha actual y vuestro nombre.

clear

ls -l
date
echo "Rojohn"
